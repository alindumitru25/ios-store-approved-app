import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontFamily: "Raleway",
    fontWeight: "300",
    fontSize: 27,
    color: "white"
  }
});

export default styles;
